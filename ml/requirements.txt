# VoiceGuard ML requirements
# Pin torchaudio to match torch version — mismatched versions cause DLL load errors on Windows.
torch>=2.0.0
torchaudio>=2.0.0,<3.0.0
numpy>=1.24.0
# soundfile provides the WAV backend for torchaudio on Windows (required when sox is unavailable)
soundfile>=0.12.0
# Hugging Face dependencies for Wav2Vec2-Small-AntiDeepfake
transformers>=4.40.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
